#include<bits/stdc++.h>
#include "function.h"
using namespace std;

int main() {
    long long a = 10, b = 3;
    bool x = true, y = false;

    jog_hobe(a, b);

    gun_hobe(a, b);

    vagshesh_hobe(a, b);

    soman_check(a, b);

    boro_check(a, b);

    bigORequal(a, b);

    abs_man(-a);

    min_eta(a, b);

    Sqr(a);

    cube(a);


    OR(x, y);

    XOR(x, y);

    bitwiseAND(a, b);

    bitwiseXOR(a, b);

    rightSHIFT(a, 1);

    ek_beshi(a);

    jor_check(a);


    cout <<endl<< "Happy Coding" << endl;

    return 0;
}
